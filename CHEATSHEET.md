# 一页速查表

这页只回答一个问题：现在要做一个产品级项目，我该怎么快速做技术判断？

## 选型四问

1. 团队会不会？
2. 产品现在需不需要？
3. 上线和维护成本高不高？
4. 以后能不能迁移或替换？

四个问题答不清，就先选更简单、更成熟、更便宜的方案。

## 后端怎么选

| 场景 | 推荐 | 避免 |
| --- | --- | --- |
| AI、数据、自动化、快速 API | Python / FastAPI / Django | 用 Python 单进程多线程做 CPU 密集并行 |
| 前后端 TypeScript 团队、I/O API、Webhook | Node.js / NestJS / Fastify | 把 CPU 计算塞进事件循环 |
| 高并发服务、网关、CLI、云原生工具 | Go | 用 Go 硬做复杂 CRUD 后台 |
| 企业系统、订单、支付、金融、电商 | Java/Kotlin / Spring | 还没验证产品就做重型架构 |
| 极致性能、系统组件、安全边界 | Rust | 普通业务 MVP |
| 内容站、CMS、电商、传统 Web | PHP / Laravel / WordPress | 过度依赖质量不明的插件 |
| SaaS、CRM、管理后台快速验证 | Ruby on Rails | 团队无 Ruby 经验且招聘困难 |
| 企业内部系统、Windows/Azure 生态 | C# / .NET | AI/数据生态主导的项目 |

## 前端怎么选

| 场景 | 推荐 |
| --- | --- |
| 复杂 Web App、SaaS、生态广 | React |
| 中后台、国内团队、上手快 | Vue |
| 内容站、SEO、React 生态 | Next.js |
| 内容站、SEO、Vue 生态 | Nuxt |
| 小团队轻量交互 | Svelte |
| 跨端 App，已有 React 团队 | React Native |
| 跨端 App，强 UI 一致性 | Flutter |
| 桌面工具，需要本地文件/系统能力 | Tauri / Electron |

## 数据怎么选

| 需求 | 默认选择 | 什么时候升级 |
| --- | --- | --- |
| 主业务数据库 | Postgres | 团队只熟 MySQL 或生态强依赖 MySQL 时用 MySQL |
| 原型、单机、桌面、小工具 | SQLite | 多实例写入、生产多人服务时迁 Postgres/MySQL |
| 缓存、会话、限流、短期状态 | Redis | 确实出现读压力或短期状态需求再加 |
| 简单异步任务 | 数据库任务表 | 任务稳定后 SQS/RabbitMQ |
| 业务消息队列 | RabbitMQ / SQS | 需要事件流、多消费者回放再 Kafka |
| 全文搜索 | Postgres 全文搜索 | 搜索体验成为核心后 OpenSearch/Elasticsearch |
| 分析报表 | Postgres 聚合 | 大量事件和宽表聚合后 ClickHouse |
| 图片、附件、导出、备份 | S3 / R2 / OSS / COS | 不要把大文件塞数据库 |

## 高并发先问什么

不要先问“该换什么语言”。先问：

- 目标是 QPS、延迟、连接数、峰值，还是成本？
- 请求是在等 I/O，还是在吃 CPU？
- 瓶颈在应用、数据库、缓存、队列、外部 API，还是网络？
- 能不能缓存？
- 能不能异步？
- 能不能削峰？
- 能不能限流？
- 数据库连接池、慢查询、锁、N+1 有没有问题？

公式：

```text
并发数 ≈ QPS × 平均响应时间
```

同样 1000 QPS，平均响应 0.2 秒是约 200 并发；平均响应 2 秒就是约 2000 并发。

## Python 多线程一句话

不要说“Python 不能高并发”。

更准确：

```text
CPython 的 GIL 让同一进程里的多个线程不能同时执行 Python 字节码。
所以 Python 多线程不适合 CPU 密集型并行计算。
但 I/O 型 Web 服务可以通过 async、多进程、队列、缓存、水平扩容支撑高并发。
```

## 扩容路线

1. 查瓶颈：日志、指标、慢 SQL、连接池、CPU、内存。
2. 单机优化：减少重复计算、N+1、锁、大对象。
3. 异步 I/O：适合大量等待数据库、Redis、HTTP 的请求。
4. 多进程：Python 利用多核的常见方式。
5. 水平扩容：服务无状态，文件进对象存储，session 外置。
6. 队列削峰：慢任务后台处理。
7. 缓存：读多写少和热点数据。
8. 数据库优化：索引、分页、连接池、读写分离、慢查询。
9. 限流、熔断、降级：保护核心链路。

## 容灾最低标准

- 应用至少 2 个实例。
- 进程崩了能自动重启。
- 数据库自动备份。
- 备份不和数据库放同一台机器。
- 至少每月做恢复演练。
- 所有远程调用有超时。
- 重试有次数限制和退避。
- 核心操作做幂等。
- 发布能回滚。
- 服务不可用、错误率、延迟、数据库异常能告警。
- 队列失败进死信队列。
- 第三方服务不可用有降级方案。

## 产品级最低标准

- 鉴权：后端校验权限，token 有过期时间。
- 日志：请求 ID、用户 ID、错误上下文，不能泄露敏感信息。
- 错误处理：统一错误结构，前端提示能让人看懂。
- 配置：密钥用环境变量，不提交到仓库。
- 迁移：数据库结构变更走迁移脚本。
- 备份：生产数据定期备份并验证恢复。
- 监控：可用性、错误率、延迟、数据库、关键业务事件。
- CI：提交后自动测试、构建、阻止失败发布。
- 安全：HTTPS、密码哈希、SQL 注入、XSS、CSRF、上传限制、限流。
- 隐私：最小化收集，敏感数据脱敏，日志不泄露隐私。

## 一句话结论

早期项目不是不用技术深度，而是不要把技术复杂度放错地方。

最值钱的判断是：

```text
哪些地方必须稳，哪些地方可以简单，哪些地方以后能换。
```
